'use strict';

// Uses Bones.Logger for logging
// and Sync.local.logs.setup for indexeddb
var Logger = require('../bones/').Logger,
    Sync = require('../sync');

function init() {
  return;
}

module.exports = {
  init: init
};
